# ── Service config ────────────────────────────────────────
# TODO: update SERVICE_NAME to match your service's path_prefix
SERVICE_NAME=svc
PORT=3001

# ── Database (shared postgres, separate schema) ────────────
DB_HOST=localhost
DB_PORT=5433
DB_USER=admin
DB_PASSWORD=password
DB_NAME=rmsb_db
DB_SCHEMA=schema_svc
