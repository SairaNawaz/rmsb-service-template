# ── Service config ────────────────────────────────────────
PORT=3000

# ── Database ───────────────────────────────────────────────
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=rmsb_db

DB_HOST=postgres
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=password
DB_NAME=rmsb_db
# TODO: update schema name to match your service (e.g. schema_s2)
DB_SCHEMA=schema_svc
